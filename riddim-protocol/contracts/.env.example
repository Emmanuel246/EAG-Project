# Funded HSK-testnet private key used to deploy + (optionally) seed the contract.
# Never commit the real value. HSK Testnet faucet: https://faucet.hsk.xyz
PRIVATE_KEY=0xyour_funded_testnet_private_key

# Optional overrides (sensible defaults live in hardhat.config.js).
# HSK Testnet is chain 133 at https://testnet.hsk.xyz (NOT .../rpc; 177 is mainnet).
HSK_RPC_URL=https://testnet.hsk.xyz
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
